{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aether-vault.dev/schemas/tool-manifest-1.0.json",
  "title": "Aether-Vault tool manifest (GET /api/tool-manifests/{id}, v1.3.1)",
  "description": "python/av_server/server.py::_tool_manifest_to_dict(). The registry-published INDEX ROW pointing at a manifest document; the document itself (writable_paths/network/network_destinations/gpu, see python/av_cli/sandbox/manifest.py::DEFAULT_MANIFEST) is the referenced CAS object, fetched separately via GET /api/objects/{object_id}. See docs/contracts.md.",
  "type": "object",
  "required": ["id", "project_id", "improver_id", "object_id", "created_by", "created_at"],
  "properties": {
    "id": {"type": "string"},
    "project_id": {"type": "string"},
    "improver_id": {"type": "string", "description": "Which improver version this manifest is armed for."},
    "object_id": {
      "type": "string",
      "description": "CAS object id of {writable_paths, network, network_destinations, gpu} — python/av_cli/sandbox/manifest.py."
    },
    "created_by": {"type": ["string", "null"]},
    "created_at": {"type": ["string", "null"]}
  }
}
