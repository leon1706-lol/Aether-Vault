# Fallback list of check-run names `release_gate.py` requires to be green for a tag,
# used only when the live GitHub branch-protection API call fails -- the live
# `required_status_checks` list on `master` is the primary source of truth.
#
# Deliberately excludes anything schedule-only (nightly.yml's jobs), `flaky-quarantine`
# (continue-on-error by design), and docker-edge.yml's path-filtered job. One context
# name per line; blank lines and #-comments ignored.

# tests.yml
test (3.10)
test (3.14)
test-linux (3.10)
test-linux (3.14)
plugin-tests
webui-tests
server-tests
server-tests-windows
e2e-suite
chaos-drills
e2e-engine-smoke
slim-image-smoke
contract-matrix
migrations-drill
lint-workflows
package-build
smoke-wheel-linux
smoke-sdist-windows
webui-e2e
helm-lint
ha-drill

# security.yml
python-deps
python-static
semgrep
container-image
webui-deps
gitleaks

# codeql.yml (job id "analyze", matrix key "language")
analyze (python)
analyze (javascript-typescript)
analyze (actions)
